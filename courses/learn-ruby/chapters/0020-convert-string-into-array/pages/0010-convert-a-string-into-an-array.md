String has a method `split`
that splits a string
into an array.

<codeblock language="ruby" type="lesson">
<code>
city_name = "Salt Lake City"

array = city_name.split

puts array
</code>
</codeblock>